# SQL Scripts

This folder contains the SQL scripts used to create the PostgreSQL sample databases and database objects used throughout the PostgreSQL Administration lab.

The scripts can be executed to recreate the lab environment for testing, backup and recovery, replication, maintenance, and performance exercises.
